# Kanban Board

A simple Kanban board built with the MERN stack (MongoDB, Express, React, Node).

Create boards, add tasks and move them between `TODO`, `IN PROGRESS` and `DONE`.

## Structure

```
client/   React front end
server/   Express REST API
```

Setup instructions are added in the docs commit.
